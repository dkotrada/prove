spring:
  application:
    name: prove
  datasource:
    url: jdbc:sqlite:./db/prove-sqlite3.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update